# Ozon Parser - Работающие Решения

## 📋 Цель проекта

Собрать товары с Ozon, сохранять полные данные (отзывы/рейтинг/доставку), вести ежедневную динамику и показывать на странице `max.gogocrm.ru/ozon` с сортировкой по количеству отзывов.

---

## ✅ РАБОЧИЕ РЕШЕНИЯ

### 1. Сбор ПОЛНЫХ данных через AppleScript (ГЛАВНОЕ РЕШЕНИЕ)

**Статус:** ✅ **РАБОТАЕТ**
**Дата:** 2025-11-02
**Метод:** Множественные простые AppleScript запросы к Chrome

#### Что собирает:
- ✅ ID товара
- ✅ URL товара
- ✅ Название
- ✅ Цена
- ✅ Рейтинг
- ✅ Количество отзывов
- ✅ Дата доставки

#### Файлы:

**`collect_full_data.sh`** - Сбор с одного источника
```bash
cd /Users/mikhailzhirnov/claude/ozonparser/js
./collect_full_data.sh "/seller/guangzhouganxinmaoyidian-3366398" 10
```

**`mass_collect_all.sh`** - Массовый сбор со всех 23 источников
```bash
./mass_collect_all.sh
```

#### Как работает:
1. Открывает страницу в обычном Chrome (не Puppeteer)
2. Скроллит страницу N раз
3. Для каждого товара делает 6 простых JavaScript запросов отдельно
4. Сохраняет результат в JSON

#### Результаты тестирования:
- ✅ 30 товаров за 2 минуты
- ✅ Все поля корректны
- ✅ Обход блокировки Ozon (обычный браузер)

#### Следующие шаги:
1. Запустить массовый сбор: `./mass_collect_all.sh`
2. Объединить результаты
3. Загрузить на сервер
4. Импортировать в MongoDB

---

### 2. Поиск по изображению через Safari

**Статус:** ✅ **РАБОТАЕТ**
**Дата:** 2025-11-01
**Файл:** `js/ozon_image_search.applescript`

#### Использование:
```bash
cd /Users/mikhailzhirnov/claude/ozonparser/js

# Поиск одного изображения
osascript ozon_image_search.applescript "https://ir.ozone.ru/s3/multimedia-1-5/wc600/7249026173.jpg"

# Через Node.js wrapper
node image_search.js "URL_изображения"

# HTTP API сервер
node image_search_server.js
# Открыть http://localhost:3001
```

#### Требования:
- Safari (не Chrome!)
- Включить: Safari → Develop → Allow JavaScript from Apple Events
- Или: `defaults write com.apple.Safari AllowJavaScriptFromAppleEvents -bool true`

#### Результаты:
- Находит 25-30 похожих товаров
- Возвращает JSON с ID, названиями, URL

---

### 3. Массовый поиск похожих товаров

**Статус:** ✅ **РАБОТАЕТ**
**Файлы:** `js/bulk_image_search.js`, `js/save_to_mongodb.js`

#### Использование:
```bash
# Поиск для всех товаров из JSON
PRODUCTS_FILE=products.json node bulk_image_search.js

# Сохранение в MongoDB
MONGODB_URI="mongodb://..." node save_to_mongodb.js
```

#### Производительность:
- 3-6 товаров в минуту
- 100 товаров: ~20-30 минут
- 1000 товаров: ~3-5 часов

---

## 🗄️ MongoDB API

**URL:** https://max.gogocrm.ru/ozon/api/

### Endpoints:

**GET /api/health** - Health check
**GET /api/stats** - Статистика
**GET /api/products** - Список товаров (с фильтрами, сортировкой, пагинацией)
**GET /api/products/<id>** - Конкретный товар

### Параметры:
- `q` - поиск
- `brand` - фильтр по бренду
- `min_rating` - минимальный рейтинг
- `sort` - сортировка (updated, rating, reviews, name)
- `page`, `per_page` - пагинация

---

## 🚫 НЕ РАБОТАЕТ (не использовать)

### ❌ Puppeteer с прокси
**Проблема:** Детектируется Ozon (HTTP 403) даже с:
- puppeteer-extra-plugin-stealth
- Реальным Chrome profile
- Куками
- Резидентными прокси

**Вывод:** Ozon блокирует Puppeteer на уровне детекта автоматизации, не только по IP.

### ❌ Сложный JavaScript в AppleScript
**Проблема:** Проблемы экранирования кавычек при многострочном коде.

**Ошибки:**
```
168:170: syntax error: A number can't go after this """. (-2740)
```

**Решение:** Использовать простые отдельные запросы (как в `collect_full_data.sh`).

---

## 📝 Важные команды

### Запуск сбора данных (локально на Mac):
```bash
cd /Users/mikhailzhirnov/claude/ozonparser/js
./mass_collect_all.sh
```

### Объединение результатов:
```bash
python3 -c "import json, glob; print(json.dumps({'total': sum(json.load(open(f))['total'] for f in glob.glob('results/*_full.json')), 'products': [p for f in glob.glob('results/*_full.json') for p in json.load(open(f))['products']]}, ensure_ascii=False, indent=2))" > results/all_products.json
```

### Загрузка на сервер:
```bash
scp -P 2209 results/all_products.json root@max.gogocrm.ru:/home/ozon-parser/
```

### Импорт в MongoDB (на сервере):
```bash
ssh -p 2209 root@max.gogocrm.ru
cd /home/ozon-parser
python3 import_products.py
```

---

## 🔧 Технические детали

### Селекторы товаров:
```javascript
// Карточка
document.querySelector('[data-index="0"]')

// Ссылка
tile.querySelector('a[href*="/product/"]')

// ID из URL
link.href.match(/product\/[^\/]*-(\d+)/)

// Тексты
tile.querySelectorAll('span')

// Кнопка доставки
tile.querySelectorAll('button')
```

### Логика извлечения:
- **Название:** Самый длинный текст > 10 символов без спецсимволов
- **Цена:** Первый текст с "₽" и цифрами
- **Рейтинг:** Число формата "X.X" (0-5)
- **Отзывы:** Число из текста "N отзыв"
- **Доставка:** Текст кнопки с месяцем или "завтра"

---

## 📊 Источники данных

Всего **23 источника** (из https://max.gogocrm.ru/ozon/settings):

1. izbrannye-tovary-iz-univermaga
2. tengliqing
3. mingkang-e-commerce-co-ltd
4. qiulihong
5. tingdong
6. aoxinjie
7. i-like-you-66
8. tochka-schastya
9. mingxuanxiaodian
10. boutique-firm
11. xiaodian
12. yy-shop
13. magiya
14. dark-palace
15. jingfeng
16. feng-store
17. wujingjing3dian
18. guangzhouganxinmaoyidian
19. uilc
20. zavodskoy-magazin
21. hengkk
22. zl
23. smart-open

**Ожидаемый результат:** 500-1000 товаров
**Время сбора:** 30-60 минут

---

## 🤖 АВТОМАТИЧЕСКИЙ WORKFLOW ПОСЛЕ ПАРСИНГА

### После выполнения `./mass_collect_all.sh` АВТОМАТИЧЕСКИ делать:

**1. Объединить результаты в один JSON:**
```bash
cd /Users/mikhailzhirnov/claude/ozonparser/js

python3 -c "
import json, glob

all_products = []
for file_path in sorted(glob.glob('results/*_full.json')):
    try:
        with open(file_path, 'r', encoding='utf-8') as f:
            data = json.load(f)
            if data.get('success') and 'products' in data:
                all_products.extend(data['products'])
    except: pass

with open('results/all_products.json', 'w', encoding='utf-8') as f:
    json.dump({
        'success': True,
        'total': len(all_products),
        'products': all_products
    }, f, ensure_ascii=False, indent=2)

print(f'✅ Объединено: {len(all_products)} товаров')
"
```

**2. Загрузить на сервер:**
```bash
scp -P 2209 results/all_products.json root@max.gogocrm.ru:/home/ozon-parser/
```

**3. Импортировать в MongoDB:**
```bash
ssh -p 2209 root@max.gogocrm.ru "cd /home/ozon-parser && python3 import_products.py all_products.json"
```

**4. Проверить результаты:**
```bash
# Локально
open https://max.gogocrm.ru/ozon/

# На сервере
ssh -p 2209 root@max.gogocrm.ru "mongo ozon --eval 'db.products.count()'"
```

### Важные заметки:
- ✅ **НЕ СПРАШИВАТЬ** подтверждение - выполнять автоматически
- ✅ Импорт использует upsert - старые товары обновляются, новые добавляются
- ✅ Файл `import_products.py` уже на сервере
- ⏱️ Весь процесс занимает 1-2 минуты после завершения парсинга

---

## 📸 Поиск по изображению - ВАЖНЫЕ ОГРАНИЧЕНИЯ

### ⚠️ Известная проблема: Даты доставки

**Проблема:** При поиске по изображению через Safari поле `delivery_days` остается пустым.

**Причина:** Страница результатов поиска по изображению показывает упрощенные карточки товаров без информации о доставке. Это ограничение самого Ozon, а не нашего скрипта.

**Решение:**
- ✅ Для массового сбора товаров используйте `./mass_collect_all.sh` (Chrome + категории/селлеры) - там даты доставки собираются
- ✅ Поиск по изображению используйте только для нахождения похожих товаров (ID, название, URL, цена, рейтинг, отзывы)
- ❌ НЕ полагайтесь на `delivery_days` из результатов поиска по изображению

**Скрипт для справки:** `js/ozon_image_search_full.applescript` - собирает максимум данных, но Ozon не показывает доставку на странице поиска по фото.
